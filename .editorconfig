root = true

[*.{kt,kts}]
indent_style = space
indent_size = 4
max_line_length = 120

# Use ktlint official code style
ktlint_code_style = ktlint_official

# Explicitly forbid wildcard imports (should be enabled by default, but we set it explicitly)
ktlint_standard_no-wildcard-imports = enabled
